constexpr int mode = 1e9+7;

class Solution {
public:
    int numWays(int n) {
        if (n < 2) return 1;
        vector<int> dp(n+1, 1);
        for (int i = 2; i <= n; ++i) {
            dp[i] = (dp[i-1] + dp[i-2]) % mode;
        }
        return dp[n];
    }
};
