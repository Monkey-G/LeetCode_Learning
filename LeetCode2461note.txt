（1）对于显然的滑动窗口的问题，要用左右索引配合限定窗口来完成
（2）对于不能重复数等这类明显要查表的问题，那就意味着有查表的需要，如果是使用vector索引必须从头起效率很低，因此该用哈希表（一些特殊情况也可以用位操作来做巧妙解）
（3）在大规模计算问题中，如果求和求积之类的操作之前的操作有部分可以实现，不妨将其作为整个class的global，而非每次继承之前操作的可用部分而不是都从头计算，这样可以减少运行时间
