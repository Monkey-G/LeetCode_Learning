class Solution {
public:
    int hammingWeight(uint32_t n) {
        int res = 0;
        uint32_t k = 1;
        for (int i = 0; i < 32; ++i) {
            if (k & n) ++res;
            //cout << (k & n) << " ";
            k <<= 1;
        }
        return res;
    }
};
